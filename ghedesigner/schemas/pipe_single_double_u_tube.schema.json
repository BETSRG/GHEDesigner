{
    "type": "object",
    "properties": {
        "inner_radius": {
            "type": "number",
            "minimum": 0,
            "units": "m"
        },
        "outer_radius": {
            "type": "number",
            "minimum": 0,
            "units": "m"
        },
        "shank_spacing": {
            "type": "number",
            "minimum": 0,
            "units": "m"
        },
        "roughness": {
            "type": "number",
            "minimum": 0,
            "units": "m"
        },
        "conductivity": {
            "type": "number",
            "minimum": 0,
            "units": "W/m-K"
        },
        "rho_cp": {
            "type": "number",
            "minimum": 0,
            "units": "J/m^3-K"
        },
        "arrangement": {
            "type": "string",
            "enum": ["SINGLEUTUBE", "DOUBLEUTUBE"]
        }
    },
    "required": [
        "inner_radius",
        "outer_radius",
        "shank_spacing",
        "roughness",
        "conductivity",
        "rho_cp",
        "arrangement"
    ]
}
