{
  "type": "object",
  "properties": {
    "buried_depth": {
      "type": "number",
      "minimum": 0,
      "units": "m",
      "description": "Depth of top of borehole below the ground surface."
    },
    "radius": {
      "type": "number",
      "units": "m",
      "description": "Radius of borehole."
    }
  },
  "required": [
    "buried_depth",
    "radius"
  ]
}
