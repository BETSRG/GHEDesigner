{
  "type": "object",
  "properties": {
    "length": {
      "type": "number",
      "minimum": 0,
      "units": "m",
      "description": "Horizontal side-length of property boundary defining surface\n\narea avaliable for ground heat exchanger."
    },
    "b": {
      "type": "number",
      "minimum": 0,
      "units": "m",
      "description": "Borehole-to-borehole spacing."
    },
    "max_height": {
      "type": "number",
      "minimum": 0,
      "units": "m",
      "description": "Maximum height, or active length, of each borehole heat exchanger."
    },
    "min_height": {
      "type": "number",
      "minimum": 0,
      "units": "m",
      "description": "Minimum height, or active length, of each borehole heat exchanger."
    },
    "method": {
      "type": "string",
      "const": "NEARSQUARE",
      "description": "Design algorithm specified."
    }
  },
  "required": [
    "length",
    "b",
    "max_height",
    "min_height",
    "method"
  ]
}
